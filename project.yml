# Generated by AppFactoryKit appgen.
name: HadithReader
options:
  bundleIdPrefix: com.zubeid
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  AppFactoryKit:
    url: https://github.com/ZubeidHendricks/AppFactoryKit
    from: "0.1.0"

targets:
  HadithReader:
    type: application
    platform: iOS
    sources:
      - Sources
      - Resources
    dependencies:
      - package: AppFactoryKit
        product: AppFactoryKit
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: "Hadith Reader"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        ITSAppUsesNonExemptEncryption: false
    settings:
      base:
        PRODUCT_NAME: HadithReader
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.hadithreader
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_STYLE: Automatic

  HadithReaderTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
      - path: Sources/Hadiths.swift
      - Resources
    settings:
      base:
        PRODUCT_NAME: HadithReaderTests
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.hadithreader.tests
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"

schemes:
  Tests:
    build:
      targets:
        HadithReaderTests: [test]
    test:
      targets:
        - HadithReaderTests
